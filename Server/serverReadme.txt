The files in this folder are for running an example.


The folders inside sensor code is the actual project
To run the actual code with a sensor, have the computer that downloads this
path to SensorCode/BB_RF_AMP_LOGGER/bin/Debug/t/
Run the python server there.
And then run the visual studio program in debug mode.
